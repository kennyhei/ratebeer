<%= form_for(@style) do |f| %>
    <% if @style.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@style.errors.count, "error") %> prohibited this brewery from being saved:</h2>

          <ul>
            <% @style.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <table>
      <% if @style.new_record? %>
          <tr>
            <td>Name:</td>
            <td><%= f.text_field :name %></td>
          </tr>
          <tr>
      <% end %>
        <td>Description:</td>
        <td><%= f.text_area :description %></td>
      </tr>
      <tr>
        <td></td>
        <td><%= f.submit %></td>
      </tr>
    </table>
<% end %>